# markov-chain-simulator
A Markov chain is a mathematical system that models a sequence of events where the probability of each event depends only on the current state — not on the history of how you got there. This is called the Markov property (or "memorylessness").
